**Exemplo Resolvido: Calculando a Área de um Retângulo com Variáveis e Tipos de Dados em Python**

**1. Enunciado do Problema/Tarefa:**

Escreva um programa em Python que calcule a área de um retângulo. O programa deve definir valores para o comprimento e a largura do retângulo, armazenando-os em variáveis apropriadas. Em seguida, deve "calcular" a área utilizando esses valores e exibir o resultado final.

**2. Resolução Comentada, Passo a Passo:**

Para resolver este problema, vamos seguir três passos principais: definir as dimensões, realizar o cálculo e exibir o resultado.

```python
# --- Passo 1: Definir as dimensões do retângulo usando variáveis ---
# Neste passo, criamos variáveis para armazenar o comprimento e a largura do nosso retângulo.
#
# Conceito: Variáveis são nomes que atribuímos a locais de memória para guardar dados.
# Podemos pensar nelas como "caixas" rotuladas onde guardamos informações.
#
# Conceito: Tipos de Dados definem a natureza dos dados que uma variável pode armazenar.
# Para as dimensões de um retângulo, que podem ter casas decimais, o tipo de dado mais adequado
# é o '
